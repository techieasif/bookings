{{template "base" .}}

{{define "content"}}

    <div class="container">
        <div class="row">
            <h1>This is my about page generated by golang.</h1>
            <p>Woww!!.</p>
        </div>
        <p>This is from Template Data ::>>> {{index .StringMap "test"}}</p>


        <p>
            {{if ne (index .StringMap "remote_ip") ""}}
                Your Remote ip address if {{index .StringMap "remote_ip"}}
            {{else}}
                I don't know your ip address yet, Visit <a href="/">Home Page</a> So I can get it.
            {{end}}

        </p>
    </div>

{{end}}